<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Eve Quantum - Beta {% block title %}{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css')}}">
        <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
        <script src="https://kit.fontawesome.com/0edd732d29.js" crossorigin="anonymous"></script>
        {% block stylesheets %}{% endblock %}
    </head>

    <body class="container">

        {% if app.request.attributes.get('_route') != 'admin_login' %}
        <header class= "sticky-top">
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
                <a class="navbar-brand" href="{{ path('main_home') }}">Eve Quantum - Beta</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav mr-auto">
                    <li class="nav-item {% if app.request.attributes.get('_route') == 'question_add' %} active {% endif %}">
                        <a class="nav-link" href="{{path('question_add')}}">Poser une Question{#<span class="sr-only">(current)</span>#}</a>
                    </li>
                    <li class="nav-item {% if app.request.attributes.get('_route') == 'character_profile' %} active {% endif %}">
                        <a class="nav-link" href="{{path('character_profile')}}">Mon Profil</a>
                    </li>
                    <li class="nav-item {% if app.request.attributes.get('_route') == 'question_list' %} active {% endif %}">
                        <a class="nav-link" href="{{path('question_list')}}">Questions</a>
                    </li>
                    <li class="nav-item {% if app.request.attributes.get('_route') == 'toolbox_clipboard' %} active {% endif %}">
                        <a class="nav-link" href="{{path('toolbox_clipboard')}}">LocalSpy</a>
                    </li>
                    {# <li class="nav-item {% if app.request.attributes.get('_route') == 'main_about' %} active {% endif %}">
                        <a class="nav-link" href="{{path('main_about')}}">A Propos</a>
                    </li> #}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"></a>
                        <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{path('main_about')}}">A Propos</a>
                        {# <div class="dropdown-divider"></div> #}
                        <a class="dropdown-item" href="{{path('main_suggest')}}">Suggestion</a>
                        </div>
                    </li>
                    </ul>

                    {% if app.user.portrait is defined and app.user.portrait is not null %}
                        <span>{{app.user.name}}<span/>
                        <a style="margin-right:2vw" href="{{path('character_profile')}}">
                            <img src="{{app.user.portrait}}" class="rounded-circle" style="max-height: 50px">
                        </a>
                    {% endif %}

                    {% if (is_granted('ROLE_ADMIN') or is_granted('ROLE_MODERATOR')) %}
                        <a href="{{path('admin_dashboard')}}" target="_blank" type="button" class="btn btn-link">Admin</a>
                    {% endif %}

                    {% if app.user is defined and app.user is not null %}
                        <a href="{{path('app_logout')}}" type="button" class="btn btn-danger">Déconnexion</a>
                    {% endif %}

                    {% if app.user is defined and app.user is null %}
                        <a href="{{path('app_login')}}" type="button" class="btn btn-secondary">Connexion</a>
                    {% endif %}

                    {# <form class="form-inline my-2 my-lg-0">
                    <input class="form-control mr-sm-2" type="text" placeholder="Search">
                    <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
                    </form> #}
                    
                </div>
            </nav>
        </header>
        {% endif %}

        <main class="body p-2 rounded-bottom">
        {% block body %}{% endblock %}
        </main>
        

        <footer>

        <div class="footer">
            <p>Tout contenu relatif à <a href="https://www.eveonline.com/" target="_blank">Eve Online</a> est la propriété de <a href="https://www.ccpgames.com/" target="_blank">CCP Games</a><p>
        </div>

        {% block javascripts %}{% endblock %}
            <!-- Optional JavaScript -->
            <script src="{{ asset('assets/js/boostrap.js') }}"></script>
            <script src="{{ asset('assets/js/source/Application.js') }}"></script>
            <script src="{{ asset('assets/js/source/Utils/BaseUrl.js') }}"></script>
            <!-- jQuery first, then Popper.js, then Bootstrap JS -->
            <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
            <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
            <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
        </footer>

    </body>
</html>
