<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Eve Quantum {% block title %}{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css')}}">
        <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
        {% block stylesheets %}{% endblock %}
    </head>

    <body class="container">
        <header>
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="">Eve Quantum</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse d-flex" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                {# On utilise le characterOwnerHash mis en session à la connexion pour déterminé si l'utilisateur est bien connecté #}
                <li class="nav-item">
                    <a class="nav-link" href="">Poser une Question</a>
                </li>
                {# exemple for active button
                <li class="nav-item{% if app.request.attributes.get('_route') == 'question_add' %} active {% endif %}">
                    <a class="nav-link" href="{{path('question_add')}}">Poser une Question</a>
                </li> #}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('character_details')}}">Mon Profil</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="">Questions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('main_about')}}">A Propos</a>
                </li>
                    <a href="{{path('app_logout')}}" type="button" class="btn btn-danger">Déconnexion</a>
                </ul>
            </div>
            </nav>
        </header>

        <main class ="jumbotron">
        {% block body %}{% endblock %}
        </main>

        {% block javascripts %}{% endblock %}
        <footer>

        <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>

        </footer>
    </body>
</html>
